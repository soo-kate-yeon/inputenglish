// @MX:NOTE: Re-exports useStudyStore from web-specific stores instantiation.
// Implementation has moved to @shadowoo/shared (createStudyStore factory).
// Web instantiation is in src/lib/stores.ts.
export { useStudyStore } from '@/lib/stores';
